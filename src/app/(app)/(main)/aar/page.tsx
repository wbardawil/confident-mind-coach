export default function AarPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold tracking-tight">
        After Action Review
      </h1>
      <p className="mt-2 text-muted-foreground">
        Learn from what happened, so what it means, and what&apos;s next.
      </p>
    </div>
  );
}
